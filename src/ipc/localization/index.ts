import {
  commit,
  getState,
  initialize,
  listOptions,
  prepare,
  preview,
  restorePreview,
  revalidate,
  select,
} from "./handlers";

export const localization = {
  initialize,
  getState,
  listOptions,
  prepare,
  commit,
  select,
  preview,
  restorePreview,
  revalidate,
};
