import {
  createCloudConfig,
  deleteCloudConfig,
  listCloudConfigs,
  testCloudConnection,
  uploadPhotoToCloud,
} from "./handlers";

export const cloud = {
  createCloudConfig,
  deleteCloudConfig,
  listCloudConfigs,
  testCloudConnection,
  uploadPhotoToCloud,
};
