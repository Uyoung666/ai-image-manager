import { app } from "./app";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";
import { photos } from "./photos";

export const router = {
  theme,
  window,
  app,
  shell,
  photos,
};
