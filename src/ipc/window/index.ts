import {
  closeWindow,
  isWindowMaximized,
  maximizeWindow,
  minimizeWindow,
  setZoomFactor,
} from "./handlers";

export const window = {
  minimizeWindow,
  maximizeWindow,
  closeWindow,
  isWindowMaximized,
  setZoomFactor,
};
