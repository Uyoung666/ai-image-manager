import {
  closeWindow,
  isWindowMaximized,
  maximizeWindow,
  minimizeWindow,
} from "./handlers";

export const window = {
  minimizeWindow,
  maximizeWindow,
  closeWindow,
  isWindowMaximized,
};
