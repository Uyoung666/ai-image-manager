import {
  createFaceIdentity,
  getDetectionProgress,
  getFaceIdentity,
  listFaceIdentities,
  mergeIdentities,
  startFaceDetection,
  updateFaceIdentity,
} from "./handlers";

export const faces = {
  createFaceIdentity,
  getDetectionProgress,
  getFaceIdentity,
  listFaceIdentities,
  mergeIdentities,
  startFaceDetection,
  updateFaceIdentity,
};
