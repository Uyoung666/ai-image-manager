import {
  openExternalLink,
  openFolderDialog,
  openInExplorer,
  saveFileDialog,
} from "./handlers";

export const shell = {
  openExternalLink,
  openFolderDialog,
  openInExplorer,
  saveFileDialog,
};
