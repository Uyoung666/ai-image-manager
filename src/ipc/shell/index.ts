import {
  openExternalLink,
  openFolderDialog,
  openInExplorer,
  saveFileDialog,
  startFileDrag,
} from "./handlers";

export const shell = {
  openExternalLink,
  openFolderDialog,
  openInExplorer,
  saveFileDialog,
  startFileDrag,
};
