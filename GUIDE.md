# AI Image Manager 使用指南

> 100% 本地 AI 图片管理器 — 无需联网，所有数据存储在你自己的电脑上。

---

## 目录

- [快速入门](#快速入门)
- [照片浏览](#照片浏览)
- [搜索](#搜索)
- [AI 智能索引](#ai-智能索引)
- [标签管理](#标签管理)
- [人脸识别](#人脸识别)
- [相册与智能相册](#相册与智能相册)
- [数据仪表盘](#数据仪表盘)
- [重复照片检测](#重复照片检测)
- [批量操作](#批量操作)
- [导出与分享](#导出与分享)
- [云同步](#云同步)
- [回收站](#回收站)
- [设置](#设置)
- [键盘快捷键](#键盘快捷键)
- [常见问题](#常见问题)

---

## 快速入门

### 1. 添加照片文件夹

首次打开应用，点击 **"添加文件夹"** 按钮，选择你存放照片的目录。

应用会扫描文件夹中的所有图片（支持 JPG、PNG、RAW、HEIC、WebP、AVIF 等 30+ 格式），生成缩略图并提取 EXIF 信息。

### 2. 等待扫描完成

扫描进度会显示在侧边栏和右下角状态栏。扫描过程中可以正常使用应用，扫描完成后照片会出现在主页网格中。

### 3. 启用 AI 索引

为了让自然语言搜索（如"去年秋天的红叶"）生效，需要完成 AI 索引。点击侧边栏的 **"开始 AI 索引"** 按钮，首次运行会自动下载 AI 模型。

**注意**：AI 模型从 HuggingFace 下载，国内用户可以在设置中将镜像源切换为 hf-mirror.com 或 modelscope.cn。

---

## 照片浏览

### 主界面

- **照片网格**：瀑布流布局，按日期分组显示
- **排序方式**：日期（新→旧 / 旧→新）、文件名（A→Z / Z→A）、文件大小（大→小 / 小→大）
- **列宽调节**：工具栏右侧滑块，范围 140-320px，偏好会自动记住
- **无限滚动**：向下滚动自动加载更多照片

### 选择照片

| 操作 | 方式 |
|------|------|
| 单选 | 点击照片 |
| 多选 | `Ctrl` + 点击 |
| 范围选择 | `Shift` + 点击 |
| 全选 | `Ctrl + A` |
| 框选 | 在空白区域拖拽鼠标 |

选中照片后底部会弹出操作栏，可进行收藏、添加到相册、导出、重命名等操作。

### 照片详情面板

选中照片后按 **`I`** 键打开右侧详情面板，显示：
- 基本信息和 EXIF 元数据（相机、镜头、焦距、光圈、快门、ISO、GPS）
- 标签管理（添加、删除、AI 建议标签）
- 文件路径

详情面板宽度可拖拽调整，偏好会自动记住。

### 快速预览

选中照片后按 **`Space`** 打开快速预览，支持：
- 滚轮缩放（0.5x - 5x）
- 拖拽平移（缩放 >1x 时）
- 双击切换 2x 缩放
- 方向键切换上一张/下一张

### 灯箱

双击照片或按 `Enter` 打开全屏灯箱，支持：
- 无限轮播浏览
- 幻灯片放映（`Space` 切换播放/暂停，间隔 3/5/10 秒可选）
- 缩略图导航条
- 全屏模式

---

## 搜索

搜索栏位于主页顶部，支持三种搜索模式：

### 1. 语义搜索（文本搜图）

在搜索框输入自然语言描述，如：
- "落日海滩"
- "秋天的红叶"
- "猫咪特写"

AI 会理解语义含义并返回匹配的照片。结果按相似度排序，左上角显示匹配度徽章。

### 2. 以图搜图

将图片文件拖拽到搜索栏，或点击搜索栏右侧的图片按钮选择文件。AI 会找到视觉上最相似的照片。

### 3. EXIF 筛选

点击搜索栏右侧的筛选按钮，按以下条件精确筛选：
- **日期范围**：输入 `YYYY-MM-DD` 格式的起止日期
- **相机型号**：输入时自动提示已索引的相机型号
- **ISO 范围**：最小/最大值
- **光圈范围**：最小/最大 f/值
- **焦距范围**：最小/最大值

筛选条件以芯片形式显示，可单独移除或一键清除全部。

### 搜索历史

搜索框聚焦时会显示最近搜索记录。点击"清除全部"可一键清空。

---

## AI 智能索引

### 什么是 AI 索引？

AI 索引用 CLIP 模型将照片转换为向量嵌入，使语义搜索和以图搜图成为可能。所有计算在你的电脑上完成，不上传任何数据。

### 三种状态

| 状态 | 说明 |
|------|------|
| **未开始** | 显示"开始 AI 索引"按钮 |
| **进行中** | 显示进度条，可暂停/恢复 |
| **已完成** | 显示"索引新照片"按钮，新添加的照片可增量索引 |

### 镜像设置

如果 HuggingFace 下载慢或失败：
1. 进入 **设置 → AI 模型镜像设置**
2. 将镜像源切换为 `hf-mirror.com (推荐)` 或 `modelscope.cn`
3. 点击保存

---

## 标签管理

### 添加标签

有三种方式为照片添加标签：
1. **侧边栏拖拽**：将照片拖到侧边栏标签树上
2. **详情面板**：在右侧详情面板中搜索或创建标签
3. **AI 批量生成**：点击侧边栏标签区域的"批量生成 AI 标签"，为所有照片自动生成标签

### 标签层级

支持父子标签层级结构。右键标签 → "创建子标签"可建立层级关系。

### AI 标签建议

在详情面板中点击"分析建议标签"，AI 会分析照片内容并给出带置信度的标签建议。点击建议即可确认添加。已分析过的照片切换回来时建议会保留（无需重新分析）。

---

## 人脸识别

### 启动检测

1. 打开 **人物** 页面（侧边栏底部导航）
2. 点击 **"开始人脸检测"**
3. 等待扫描完成，进度每 2 秒更新

### 管理人物

| 操作 | 方式 |
|------|------|
| 重命名 | 点击人物卡片上的名称直接编辑，Enter 保存 |
| 合并 | 进入选择模式，勾选多个人物 → "合并为同一人" |
| 删除 | 右键人物 → 删除 |
| 查看照片 | 点击卡片进入详情页 |
| 从人物移除 | 在详情页右键某张照片 → "从此人物分组中移除" |

### 重新聚类

如果自动分组不准确，点击 **"重新聚类"** 仅重新运行聚类算法（不重新扫描）。如需完全重来，点击 **"重新扫描"**（清除所有数据后重新检测）。

---

## 相册与智能相册

### 普通相册

在 **相册** 页面点击"新建相册"，输入名称和描述创建。通过以下方式添加照片：
- 选中照片 → 底部操作栏 → "添加到相册"
- 右键照片 → "添加到相册"
- 拖拽照片到侧边栏相册名上

### 智能相册

智能相册根据规则自动匹配照片，无需手动添加。

**可用规则**：日期范围、相机型号、镜头型号、标签、焦距、光圈、文件格式

**预设模板**：
- 最近 7 天 / 30 天
- 今年
- 去年今日

创建时实时预览匹配照片数量。

---

## 数据仪表盘

打开 **仪表盘** 查看照片库的统计分析：

| 区域 | 内容 |
|------|------|
| **概览卡片** | 照片总数、AI 已处理数、时间跨度、平均 ISO |
| **相机/镜头** | 使用频率排行柱状图 |
| **焦段/光圈/ISO** | 分布直方图 |
| **拍摄时间** | 24 小时分布、年度趋势、月度分布 |
| **色彩分析** | 全局调色板、12 色相分布、饱和度分类（高/中/低） |
| **地图** | GPS 定位照片的地图标记（支持在线/离线模式） |

**联动搜索**：点击图表中的任意柱子或色块，会自动跳转到主页并应用对应的筛选条件。

---

## 重复照片检测

### 扫描

打开 **重复照片** 页面，应用会自动扫描并分组展示。

### 三种匹配类型

| 类型 | 说明 |
|------|------|
| **完全相同** | 文件内容完全一致 |
| **视觉相同** | AI 视觉判定为同一张图 |
| **高度相似** | 感知哈希判断，可能只是连拍或微调 |

### 保留策略

- **手动选择**：逐组决定保留哪张
- **保留更大文件**：自动建议删除文件较小的
- **保留更早创建**：自动建议删除较新的

选中要删除的照片后点击"删除选中"统一清理。也可以对某组点"忽略"跳过。

---

## 批量操作

选中多张照片后，底部操作栏提供：

| 操作 | 快捷键 | 说明 |
|------|--------|------|
| 收藏 | `F` | 批量切换收藏状态 |
| 添加到相册 | — | 添加到已有或新建相册 |
| 导出 | `Ctrl+Shift+E` | 导出为 ZIP，可选压缩参数 |
| 重命名 | `F2` | 按模板批量重命名 |
| 格式转换 | `Ctrl+Shift+C` | 批量转换为 WebP/AVIF/JPEG/PNG |
| 删除 | `Delete` | 移动到回收站（30 天内可恢复） |

### 批量重命名模板

支持以下变量：
- `{yyyy}` `{mm}` `{dd}` — 日期
- `{camera}` — 相机型号
- `{iso}` `{focal}` — 拍摄参数
- `{index}` `{index:N}` — 序号
- `{orig}` — 原始文件名
- `{ext}` — 扩展名

预设模板：`日期_相机_序号`、`日期_序号`、`原始名_日期`、`日期_原始名`。

---

## 导出与分享

### 导出 ZIP

按 `Ctrl+Shift+E` 或从操作栏进入。两种模式：
- **原图**：不压缩，直接打包
- **压缩**：可调节质量（10-100%）和最大宽度（640-3840px）

### 分享页面

生成一个包含缩略图、EXIF 信息和标签的单文件 HTML 页面。上传到云存储后生成分享链接，接收方在浏览器中即可查看。

### 水印

在 **设置 → 水印设置** 中配置文字或图片水印，导出和格式转换时会自动添加。支持 9 个位置锚点、边距、透明度、字号调节。

---

## 云同步

### 支持的提供商

- **WebDAV**：兼容坚果云等 WebDAV 服务
- **Amazon S3**：兼容 AWS S3 及其他 S3 兼容存储

### 配置

1. 进入 **设置 → 云同步配置**
2. 点击"添加配置"
3. 填写连接信息，点击"测试"验证
4. 保存

配置后可：
- 上传照片到云端（选中照片 → 上传）
- 生成分享页面并发布到云端

---

## 回收站

删除的照片会进入回收站，**保留 30 天**后自动永久删除。

| 操作 | 说明 |
|------|------|
| 恢复 | 选中照片 → 恢复，照片回到原来的位置 |
| 永久删除 | 选中照片 → 永久删除，不可恢复 |
| 清空 | 一键删除回收站中所有照片 |

---

## 设置

### 外观
- **主题**：深色 / 浅色 / 跟随系统
- **语言**：中文 / English

### 索引
- **缩略图缓存**：查看缓存大小和位置，可一键清除
- **无效索引清理**：清理源文件已移动或删除的索引记录

### 数据目录
- 更改应用数据存储位置（数据库、缩略图、AI 模型等）
- 迁移时显示进度，迁移期间不要关闭应用

### AI 模型镜像
- 自动检测 / hf-mirror.com / modelscope.cn / 官方 / 自定义
- 国内用户推荐切换为 hf-mirror.com

### 水印
- 文字水印或图片水印
- 9 个位置锚点 + 拖拽定位
- 边距、透明度、字号/图片大小可调
- Canvas 实时预览

---

## 键盘快捷键

按 **`?`** 打开快捷键帮助面板。

### 浏览

| 快捷键 | 功能 |
|--------|------|
| `Space` | 快速预览选中照片 |
| `←` `→` | 预览/灯箱中切换照片 |
| `↑` `↓` | 详情面板中切换照片 |
| `Esc` | 关闭预览/灯箱/面板 |
| 双击 | 打开灯箱 |

### 选择

| 快捷键 | 功能 |
|--------|------|
| 单击 | 选中照片 |
| `Ctrl` + 单击 | 多选/取消选中 |
| `Shift` + 单击 | 范围选择 |
| `Ctrl` + `A` | 全选 |

### 操作

| 快捷键 | 功能 |
|--------|------|
| `F` | 收藏/取消收藏 |
| `Delete` | 删除选中照片 |
| `F2` | 批量重命名 |
| `Ctrl` + `Shift` + `E` | 导出 |
| `Ctrl` + `Shift` + `C` | 格式转换 |

### 界面

| 快捷键 | 功能 |
|--------|------|
| `I` | 显示/隐藏详情面板 |
| `[` | 折叠/展开侧边栏 |
| `Ctrl` + `K` | 全局搜索（Spotlight） |
| `?` | 快捷键帮助 |

### 灯箱

| 快捷键 | 功能 |
|--------|------|
| `Space` | 播放/暂停幻灯片 |
| `Esc` | 退出灯箱 |

---

## 常见问题

### 搜索为什么没结果？

AI 索引可能尚未完成。检查侧边栏 AI 进度条状态——必须等到索引完成（或至少部分完成）后语义搜索才可用。

### AI 模型下载失败？

国内用户请进入 **设置 → AI 模型镜像**，将镜像源切换为 `hf-mirror.com (推荐)`。如果仍失败，可以设置自定义镜像地址。

### 照片文件被移动后会怎样？

应用不会跟踪文件的移动。如果照片被移动到其他位置，索引记录会标记为无效。在 **设置 → 索引 → 清理无效索引记录** 可以一键清理。

### 我的数据库/缩略图存在哪？

默认存储在应用数据目录。可以在 **设置 → 数据目录** 查看和更改位置，支持迁移到其他盘符。

### 人脸检测不准确？

可以尝试"重新聚类"来改善分组效果，或者手动合并/拆分人物。检测阈值固定为 0.55，这是一个平衡准确率和召回率的值。

### 重复检测没发现重复？

重复检测依赖 pHash 感知哈希和 CLIP 视觉相似度比较，需要在 AI 索引完成后才能进行视觉重复检测。完全相同的文件在任何时候都可以检测。

### 回收站的照片能保存多久？

30 天。超过 30 天自动永久删除。

### 支持哪些图片格式？

JPG、JPEG、PNG、WebP、AVIF、HEIC、HEIF、TIFF、BMP、GIF、RAW（CR2/CR3/NEF/ARW/DNG/RAF/RW2/ORF 等），共 30+ 种格式。RAW 格式通过 sharp 引擎转换为预览图。
