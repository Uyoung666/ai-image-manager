# AI Image Manager v2.0.0 使用指南

> 面向 Windows 的本地优先照片管理工具：在现有文件夹上建立索引，用搜索、整理、选片和分析能力管理自己的图库。

本指南按实际使用顺序编排。v2.0.0 是新的公开发布起点；本文只描述当前版本行为，不对历史版本作升级兼容或数据格式承诺。

---

## 目录

- [开始之前](#开始之前)
- [快速入门](#快速入门)
- [照片浏览](#照片浏览)
- [搜索](#搜索)
- [文件夹导航](#文件夹导航)
- [AI 智能索引](#ai-智能索引)
- [自定义 AI 模型](#自定义-ai-模型)
- [标签管理](#标签管理)
- [人物管理](#人物管理)
- [相册与智能相册](#相册与智能相册)
- [序列管理](#序列管理)
- [漫游](#漫游)
- [数据仪表盘](#数据仪表盘)
- [照片选片](#照片选片)
- [重复照片检测](#重复照片检测)
- [批量操作](#批量操作)
- [导出、分享与水印](#导出分享与水印)
- [云存储](#云存储)
- [回收站](#回收站)
- [设置](#设置)
- [隐私、网络与诊断](#隐私网络与诊断)
- [键盘快捷键](#键盘快捷键)
- [常见问题](#常见问题)

---

## 开始之前

AI Image Manager 直接读取你选择的照片文件夹。它会在应用数据目录中生成数据库、缩略图、向量索引、AI 模型缓存和日志，但不会因为导入而自动搬走或复制原始照片。

### 支持的系统

- Windows 10 / 11，64 位
- x64 处理器
- 建议 8 GB 及以上内存；大型图库建议 16 GB 及以上
- 建议使用 SSD，并为派生数据预留空间
- DirectML GPU 可选；不支持时使用 CPU

实际耗时取决于图片格式、尺寸、磁盘、系统负载和硬件。目前记录的实际观测值如下，数值四舍五入到秒：

| 工作量 | 导入 | AI 嵌入 | 总耗时 |
| ---: | ---: | ---: | ---: |
| 1,000 张照片 | 30 秒 | 32 秒 | **62 秒** |
| 单日 10,000 张 JPG 照片 | 约 340 秒（5 分 40 秒） | 约 746 秒（12 分 26 秒） | **约 1,086 秒（18 分 06 秒）** |

单日 10,000 张测试包含导入、缩略图/EXIF/哈希准备和 SigLIP 嵌入；测试后的派生数据已清理。该结果只是当前版本的一次性能观测，不是固定性能承诺。

### 文件格式

应用支持 JPG、JPEG、PNG、WebP、AVIF、TIFF、TIF、HEIC、HEIF、GIF、BMP、ICO，以及 CR2、CR3、NEF、NRW、ARW、SRF、SR2、DNG、ORF、RW2、RAF、PEF、RWL、3FR、RAW 等相机 RAW 格式，共 27 种扩展名。

RAW 主要通过 ExifTool 读取文件内嵌 JPEG 预览。如果 RAW 不包含可用的内嵌预览，缩略图、预览或 AI 分析可能受限。

---

## 快速入门

### 1. 首次启动引导

首次启动会显示设置向导：

1. **数据目录**：选择数据库、缩略图、向量索引、日志和模型等派生数据的存储位置。
2. **GPU 加速**：检测 DirectML 是否可用，并选择是否启用。
3. **完成**：选择中文或 English 界面并进入主页。

发布安装包包含当前版本所需的 AI 模型，通常不需要在第一次使用时额外下载。模型缺失或校验失败时，应用可能要求重新获取模型，此时需要网络连接。

数据目录迁移过程中请不要关闭应用。迁移完成后，应用会刷新索引和界面状态。

### 2. 添加照片文件夹

1. 在主页点击 **添加文件夹**。
2. 选择包含照片的目录，或将文件夹拖入窗口。
3. 等待扫描队列处理文件。

导入阶段会扫描支持的扩展名、读取基础信息和 EXIF、生成缩略图，并建立后续 AI 处理所需的任务。导入在后台执行，你可以继续查看已经处理完成的照片。

应用会记录你选择的文件夹并监听后续变化。新增或修改的文件会进入增量处理；如果文件被移动到应用无法追踪的新位置，旧索引会变为无效记录。

### 3. 查看导入进度

主页或侧边栏的进度区域会显示扫描和 AI 处理状态。导入、缩略图和 AI 嵌入是不同阶段：照片可以先被浏览，语义搜索和以图搜图则需要等待对应 AI 索引。

### 4. 建议的首次工作流

推荐按以下顺序建立图库：

1. 先添加一个较小的照片文件夹，确认路径和预览正常。
2. 等待导入完成，检查几张照片的 EXIF 和 RAW 预览。
3. 启动 AI 索引，等待进度稳定。
4. 使用自然语言搜索和以图搜图验证结果。
5. 再添加更多年份、项目或客户文件夹。
6. 最后运行人物识别、重复检测、序列识别或选片。

---

## 照片浏览

### 主界面

主页包含以下区域：

- **侧边栏**：全部照片、文件夹、标签、相册、人物、重复照片、选片、仪表盘、回收站和设置。
- **搜索栏**：语义搜索、文件名搜索、以图搜图和精确筛选。
- **照片网格**：虚拟化瀑布流，按日期分组并按需加载。
- **底部操作栏**：选中照片后显示收藏、相册、导出、重命名、转换、分享和删除等操作。

工具栏可以切换排序方式：拍摄日期、文件名和文件大小。照片列宽可以调整，应用会记住界面偏好。

### 选择照片

| 操作 | 方式 |
| --- | --- |
| 单选 | 点击照片 |
| 多选 | `Ctrl` + 点击 |
| 范围选择 | `Shift` + 点击 |
| 全选 | `Ctrl + A` |
| 框选 | 在网格空白区域拖拽 |
| 取消选择 | 点击空白区域或按 `Esc` |

选中照片后，可以使用底部操作栏或右键菜单。涉及删除、移动到回收站或批量改名的操作，请在确认前检查选择数量。

### 照片详情面板

选中照片后按 `I`，或点击详情入口，打开右侧面板。面板通常包含：

- 文件名、路径、尺寸、文件大小和拍摄时间
- 相机、镜头、焦距、光圈、快门、ISO、GPS 等 EXIF
- 标签添加、删除和 AI 标签建议
- 人物关系和相关状态

详情面板宽度可以调整。长路径和文件名会截断显示，悬停或聚焦可查看完整内容。

### 快速预览

选中照片后按 `Space` 打开快速预览：

- 使用 `←` / `→` 切换照片
- 查看文件名、尺寸和日期
- 将照片拖出到其他应用
- 复制图片或在资源管理器中定位
- 按 `Enter` 进入灯箱

### 灯箱

双击照片，或在快速预览中按 `Enter`，进入灯箱审阅：

- `Space` 播放或暂停幻灯片
- 滚轮缩放，拖拽平移，双击切换适合窗口和实际像素
- `R` 顺时针旋转，`Shift + R` 逆时针旋转
- `0` 适合窗口，`1` 查看实际像素
- `T` 显示或隐藏缩略图导航
- `I` 显示或隐藏信息面板
- 支持全屏和按时间间隔播放

灯箱只改变应用内的查看状态，不会修改原始文件。旋转和幻灯片状态也不会自动写回照片元数据。

---

## 搜索

主页搜索栏支持多种搜索方式。搜索结果可以继续叠加日期、标签和 EXIF 条件。

### 1. 语义搜索

在搜索框输入自然语言描述，例如：

- `落日海滩`
- `秋天的红叶`
- `猫咪特写`
- `sunset by the beach`

AI 会将文字转换为查询向量并按视觉语义返回结果。结果相关度会显示在照片卡片或搜索信息中。

语义搜索需要 AI 文本和图片索引。若 AI 尚未完成、模型不可用或向量库正在修复，应用会提示状态；这时仍可使用文件名、标签、人物和元数据搜索。

### 2. 以图搜图

将参考图片拖入搜索栏，或点击搜索栏中的图片按钮选择文件。应用会计算参考图的视觉特征，并在已建立向量的图库中查找相似照片。

参考图片不必先导入图库。搜索完成后可以清除参考图，恢复普通搜索。

### 3. EXIF 和精确筛选

点击筛选按钮，按以下条件组合：

- 拍摄日期范围
- 相机型号和镜头型号
- ISO 最小值/最大值
- 光圈最小值/最大值
- 焦距最小值/最大值
- 文件格式、标签、收藏等可用条件

已启用的条件会显示为筛选标签，可以单独删除或一键清除。

日期建议使用 `YYYY-MM-DD` 格式。没有对应 EXIF 的照片不会被错误填充为一个拍摄日期；筛选结果应结合数据覆盖率理解。

### 4. 文件名通配符

适合查找相机原始文件名、导出批次或自定义命名结果：

| 示例 | 含义 |
| --- | --- |
| `IMG_*.jpg` | 以 `IMG_` 开头的 JPG |
| `*2025*` | 文件名中包含 `2025` |
| `DSC_00??.ARW` | `DSC_00` 后跟任意两位字符的 ARW |

### 5. 全局搜索

按 `Ctrl + K` 打开聚光灯搜索，可以查找：

- 照片、文件夹和文件名
- 标签和相册
- 人物
- 设置页和功能入口

全局搜索适合快速跳转，不等同于主页的完整语义搜索。

### 搜索历史

聚焦搜索框时会显示最近搜索。点击 **清除全部** 可删除搜索历史。搜索历史只用于本地界面体验，不会作为诊断包内容。

---

## 文件夹导航

侧边栏文件夹树按真实目录层级展示已索引照片。父级文件夹会汇总子目录的照片数量。

### 文件夹外观

文件夹外观只影响应用内显示，不修改磁盘目录：

1. 在侧边栏右键文件夹。
2. 选择 **文件夹外观**。
3. 设置颜色和图标徽标。
4. 保存。

如果源文件被移动或删除，文件夹数量和有效记录会在重新索引、清理无效索引或刷新后更新。

---

## AI 智能索引

### AI 索引做什么

AI 索引使用本地视觉模型为照片生成向量，使文本搜图、以图搜图、视觉重复检测和部分 AI 标签能力成为可能。计算默认在本机完成。

### 索引状态

| 状态 | 说明 |
| --- | --- |
| 未开始 | 显示开始 AI 索引入口 |
| 进行中 | 显示进度，可暂停或取消 |
| 已暂停 | 保留已完成结果，可继续处理 |
| 已完成 | 新照片可以增量索引 |
| 需要修复 | 向量库或模型状态异常，按界面提示重试或重建 |

导入和 AI 索引可以分开完成。添加新文件夹后，优先使用增量索引，不必重复处理已经完成的照片。

### GPU 加速

DirectML GPU 加速可以用于图像嵌入和人脸识别：

1. 首次启动向导第 2 步选择检测 GPU。
2. 或打开 **设置 → 加速**，点击 **检测 GPU**。
3. 确认支持后开启开关。
4. 重新启动索引任务使设置生效。

GPU 探测失败、显卡不支持或运行时异常时，应用会自动使用 CPU。实际加速幅度取决于 GPU、驱动、图片和并发配置，不应把某一次设备结果当作普遍保证。

### 内置模型

当前发布包包含以下本地模型。四个模型职责不同：标准语义/视觉检索由 SigLIP（中文查询必要时由 OPUS-MT 翻译）负责，人物识别链路由 YuNet + SFace 负责。

| 模型 | 能力 | 在应用中的用途 | 不负责的任务 |
| --- | --- | --- | --- |
| **SigLIP Base Patch16-224** | 图像和文本嵌入；768 维、L2 归一化统一向量空间 | 中文/英文文本搜图、以图搜图、AI 标签建议、视觉重复检测、相似照片候选分组 | 图像生成、修图、OCR、人脸识别 |
| **OPUS-MT Chinese to English** | 中文到英文的本地查询翻译 | 将中文搜索词转换为英文，交给 SigLIP 文本嵌入以完成中文语义搜图 | 图像生成、图像/人脸特征提取、通用翻译服务 |
| **YuNet** | 人脸检测和 5 点面部关键点定位；输出人脸框和位置 | 找到照片中的人脸，为人物整理流程提供位置与关键点 | 判断人物身份、通用图像嵌入、语义搜索 |
| **SFace** | 128 维人脸特征嵌入 | 计算人脸相似度，辅助人物聚类和人物整理 | 人脸检测、通用图像嵌入、文本搜图 |

模型的完整来源、版本、用途和许可证见 [THIRD_PARTY_MODEL_NOTICES.md](THIRD_PARTY_MODEL_NOTICES.md)。

### 当前标准版模型能力

当前标准嵌入模型适配器为 `siglip-v1-base-patch16-224`（SigLIP Base Patch16-224），能力包括：

- 图像嵌入和文本嵌入：使用 768 维、L2 归一化的统一向量空间。
- 文本搜图：支持中文和英文自然语言语义搜索。
- 以图搜图：根据参考图片查找视觉相似内容。
- AI 标签建议：计算图片与候选标签之间的视觉/语义相似度。
- 视觉重复检测：生成视觉相同或视觉相似的候选分组。

当前标准模型使用正式校准的 `siglip-v1-base-patch16-224-default` 阈值 profile。中文查询进入文本搜图流程前，必要时由 OPUS-MT 在本地翻译为英文；人物相关能力由 YuNet（检测与关键点）和 SFace（人脸特征）独立完成。模型结果用于检索和辅助整理，仍应结合照片内容进行人工判断。

### 自定义 AI 模型

当前版本默认使用 **SigLIP v1** 作为标准嵌入模型。应用暂不开放外部模型导入、模型切换或面向普通用户的模型下载功能。

项目已预留可插拔模型适配层。未来可以在不修改数据库结构、不改变搜索业务逻辑和不增加默认安装包体积的情况下，接入经过适配和校准的外置模型。

自定义模型必须同时提供：

- 图像和文本嵌入能力
- 固定的向量维度和归一化规则
- 模型文件清单及 SHA-256
- 图像预处理和输出配置
- 稳定的模型指纹
- 独立的阈值配置
- 与当前模型隔离的向量索引

更换模型后不能直接复用旧向量库，必须重新建立与模型指纹匹配的向量索引。未经校准的自定义模型不会自动用于标签采纳或重复照片确认。

当前版本仅保留扩展接口，不提供自定义模型的用户界面、模型下载和安装包内置支持。自定义模型接入属于开发者能力，不能通过将模型文件放入 `models` 目录直接启用。适配器协议和验收要求见 [AI 模型适配器开发指南](docs/AI_MODEL_ADAPTER_GUIDE.md)。

---

## 标签管理

### 添加标签

可以通过三种方式管理标签：

1. 将照片拖到侧边栏标签上。
2. 在详情面板中搜索已有标签或创建新标签。
3. 使用照片或标签区域中的 AI 标签建议/批量生成入口。

标签是应用数据库中的整理信息，不会自动写入原始图片文件。

### 标签层级

右键标签并选择 **创建子标签**，可以建立父子层级，例如：

```text
旅行
├─ 日本
├─ 海边
└─ 秋季
```

### AI 标签建议

在照片详情面板中点击 **分析建议标签**：

1. 等待 AI 分析照片内容。
2. 查看带置信度的建议。
3. 点击建议确认添加，或关闭建议不做修改。

已分析的照片会保留建议状态，重新打开详情时不必立即重复分析。

---

## 人物管理

人物识别使用本地人脸检测和特征聚类。识别结果是辅助整理结果，建议在合并或批量操作前人工复核。

### 启动人脸检测

1. 打开侧边栏的 **人物** 页面。
2. 点击 **开始人脸检测**。
3. 等待检测、特征提取和聚类完成。

检测过程会显示进度。GPU 可选；不支持 DirectML 时自动回退 CPU。

### 管理人物

| 操作 | 方式 |
| --- | --- |
| 命名 | 点击人物卡片名称并编辑，按 Enter 保存 |
| 查看照片 | 点击人物卡片 |
| 合并 | 选择多个人物后点击 **合并为同一人** |
| 隐藏 | 在人物卡片的更多操作中选择隐藏 |
| 从人物移除 | 在人物详情中选择照片并移除关系 |
| 重新聚类 | 只重新运行聚类，不重新扫描照片 |
| 重新扫描 | 清除当前人脸结果后重新检测 |

自动分组可能受到遮挡、角度、光线和照片质量影响。错误分组优先使用合并、拆分、移除或重新聚类修正。

---

## 相册与智能相册

### 普通相册

在 **相册** 页面点击 **新建相册**，填写名称和可选描述。添加照片的方式：

- 主页选中照片 → 底部操作栏 → **添加到相册**
- 右键照片 → **添加到相册**
- 将照片拖到侧边栏相册

相册是应用内的组织关系，不会复制原始文件。

### 智能相册

智能相册用规则自动匹配照片，不需要手动维护成员。可用规则包括日期、相机、镜头、标签、焦距、光圈、ISO 和文件格式。

创建步骤：

1. 点击 **新建智能相册**。
2. 输入名称和描述。
3. 添加一个或多个匹配条件。
4. 查看实时匹配数量。
5. 保存。

常见预设包括最近 7 天、最近 30 天、今年和去年今日。缺少对应元数据的照片不会匹配依赖该字段的规则。

---

## 序列管理

序列用于整理连续拍摄的连拍或延时摄影照片，减少图库中的重复浏览负担。

### 识别序列

1. 打开 **设置 → 序列识别**。
2. 选择严格、平衡、宽松或自定义预设。
3. 返回照片页面或序列管理页面，点击 **识别序列**。
4. 等待应用根据时间节奏和画面相似度生成候选序列。

严格预设优先减少误判，宽松预设倾向于提高召回，平衡为默认选择。

### 查看和调整

打开序列后可以：

- 展开查看所有帧
- 播放序列
- 选择或接受推荐代表照片
- 调整序列中的帧顺序
- 从指定帧开始拆分序列
- 返回普通照片视图

序列识别只建立应用内关系，不删除照片，也不改变文件夹中的文件。

---

## 漫游

漫游用于在不进行明确搜索时重新发现图库内容。

### 手动开始

打开 **设置 → 漫游**，点击 **立即开始漫游**。漫游至少需要两张可用照片。

### 自动漫游

在漫游设置中开启 **闲置时自动漫游**，并设置：

- 闲置等待时间：10、15 或 30 分钟
- 每张照片停留时间：3、5 或 10 秒
- 内容模式：自动轮换、时光胶囊、主题放映、图库遗珠或松鼠轮屏保

自动漫游只会在应用可见、没有弹窗且没有后台任务时启动。

### 保存一轮漫游

漫游过程中可暂停、退出或保存本轮。点击 **保存本轮为相册**，应用会将本轮照片整理成一个普通相册，不会复制照片文件。

---

## 数据仪表盘

打开 **仪表盘** 查看图库统计和拍摄习惯。可选择全部、今年、近 12 个月或自定义日期范围。

| 区域 | 内容 |
| --- | --- |
| 概览 | 照片总数、当前范围、日期覆盖率、图库健康度 |
| 器材 | 品牌、相机、镜头分布 |
| 曝光 | 焦距、光圈、ISO、快门、曝光模式 |
| 拍摄技术 | 对焦、测光、白平衡、驱动模式等可用 EXIF |
| 时间 | 年度、月份、日期和一天中的拍摄时段 |
| 地点与色彩 | GPS 地图、色彩覆盖、色相和饱和度 |

点击图表中的分类、柱形或色块，可以下钻到主页并应用对应筛选。没有 EXIF 或高级元数据的照片会降低该图表的覆盖率，但不会影响照片本身的浏览。

---

## 照片选片

选片适合旅行照片、活动记录、连拍和后期前的初筛。进入侧边栏 **选片** 页面，点击 **新建选片**。

### 创建会话

填写：

- 会话名称，例如“北海道旅行精选”
- 来源文件夹或主页已选照片
- **PK / Duel 模式** 或 **甄选 / Curate 模式**
- PK 策略：快速、标准或精细
- 排序方式：按时间或按相似度

### PK / Duel 模式

两张照片并排比较：

- `←` 选择左图
- `→` 选择右图
- `Space` 或 `↓` 跳过
- `D` 标记平局
- `Ctrl + Z` 撤销
- `+` / `-` 放大或缩小
- `0` 回到适合屏幕
- 鼠标滚轮缩放，拖拽平移，双击切换实际像素

两侧可以同步缩放和平移；当照片比例差异较大时，可能无法同步。系统会根据多轮比较使用 Elo 评分形成排名。比较较长时间后会显示休息提醒。

### 甄选 / Curate 模式

逐张审阅：

- `→` 保留
- `←`、`↓` 或 `Space` 淘汰
- `S` 跳过相似照片
- `Ctrl + Z` 撤销

缩放、平移、EXIF 查看和实际像素检查与 PK 模式一致。

### 处理结果

完成后进入结果视图：

- 按评分、状态或日期排序
- 输入数量并将 Top-N 标记为保留
- 多选后批量保留、淘汰、加入相册或导出
- 将淘汰照片移入 30 天回收站
- 复制或删除选片会话；删除会话不会删除照片

选片结果是辅助决策，不会自动覆盖原始照片。

---

## 重复照片检测

### 扫描

打开 **重复照片** 页面并开始扫描。完全相同的文件可以在没有 AI 索引时处理；视觉重复和视觉相似检测需要 AI 向量或感知哈希等分析结果。

### 匹配类型

| 类型 | 说明 |
| --- | --- |
| 完全相同 | 文件内容一致 |
| 视觉相同 | 视觉模型判断为同一张图，文件内容可能不同 |
| 视觉相似 | 可能是连拍、裁剪、调色或微调后的照片 |

视觉相似组不代表应用可以替你决定保留哪一张。打开组详情，确认尺寸、EXIF、清晰度和用途后再设置保留者。

### 清理流程

1. 打开重复组详情。
2. 设置至少一张保留照片。
3. 移除误判照片或忽略整个组。
4. 点击 **清理**，确认待处理数量和预计释放空间。
5. 清理对象会进入应用回收站，不会立即永久删除。

---

## 批量操作

选中多张照片后，底部操作栏提供：

| 操作 | 快捷键 | 说明 |
| --- | --- | --- |
| 收藏 | `F` | 批量切换收藏状态 |
| 加入相册 | — | 加入已有或新建相册 |
| 导出 | `Ctrl + Shift + E` | 导出原图或压缩 ZIP |
| 批量重命名 | `F2` | 按模板生成文件名 |
| 格式转换 | `Ctrl + Shift + C` | 转换为 JPG、PNG、WebP 或 AVIF |
| 删除 | `Delete` | 移入应用回收站 |

### 批量重命名变量

- `{yyyy}`、`{mm}`、`{dd}`：拍摄日期
- `{camera}`：相机型号
- `{iso}`、`{focal}`：ISO 和焦距
- `{index}`、`{index:N}`：序号，可指定宽度
- `{orig}`：原始文件名
- `{ext}`：扩展名

可使用预设：`日期_相机_序号`、`日期_序号`、`原始名_日期`、`日期_原始名`。批量改名会修改磁盘上的文件名，执行前请检查预览和冲突提示。

---

## 导出、分享与水印

### 导出 ZIP

按 `Ctrl + Shift + E` 或从底部操作栏打开导出：

- **原图**：不重新压缩，直接打包原始文件。
- **压缩**：设置质量和最大宽度，适合交付、预览或网络分享。

导出目录由用户选择。导出不会改变图库中的原始文件。

### HTML 分享页

分享功能会生成包含缩略图、EXIF 和标签的单文件 HTML 页面。它可以先保存到本地，也可以在配置 WebDAV/S3 后主动发布到云端。

发布到云端后，链接的访问控制和数据保留由对应服务决定。不要把含有敏感位置、人物或客户信息的分享页发布到不受信任的空间。

### 水印

打开 **设置 → 水印**：

- 选择文字或图片水印
- 使用 9 个锚点或拖拽预览定位
- 调整边距、透明度、字号或图片缩放
- 保存后在导出和格式转换时应用

水印默认作用于输出文件，不会修改原图。

---

## 云存储

### 支持的提供商

- **WebDAV**：兼容坚果云等 WebDAV 服务。
- **Amazon S3 / S3 兼容存储**：支持自定义 endpoint、bucket 和访问凭据。

### 配置步骤

1. 进入 **设置 → 云同步**。
2. 点击 **添加配置**。
3. 选择 WebDAV 或 S3。
4. 填写地址、bucket、账号和凭据等信息。
5. 点击 **测试**，确认连接后保存。

### 上传和分享

配置完成后，用户可以从照片选择、右键菜单或分享对话框主动执行上传。应用不会因为保存云配置而自动上传整库。

云服务的账号权限、加密、访问控制、费用和隐私政策由服务提供商负责。不要在公共 Issue、日志或截图中暴露 access key、secret key 或 WebDAV 密码。

---

## 回收站

应用删除操作先进入应用回收站，默认保留 30 天。到期后会移动到 Windows 系统回收站；最终能否恢复取决于系统回收站状态和用户后续操作。

| 操作 | 说明 |
| --- | --- |
| 恢复 | 将照片恢复到原始位置 |
| 移入系统回收站 | 提前结束应用内保留期，交由 Windows 回收站管理 |
| 清空 | 将应用回收站中的照片批量移入系统回收站 |

如果源文件在应用外被移动或删除，应用可能无法恢复到原始位置。永久删除前务必确认备份和选择范围。

---

## 设置

### 外观与交互

- 深色、浅色或跟随系统主题
- 中文 / English
- 强调色、界面缩放和减少动画
- 搜索匹配灵敏度
- 侧边栏默认展开或折叠
- 关闭窗口时退出、最小化到托盘或询问
- 记住窗口位置和大小

### 存储与索引

- 查看和修改数据目录
- 迁移数据库、缩略图、向量索引和模型等派生数据
- 查看缩略图缓存大小和位置
- 清除缩略图缓存
- 查看数据库位置和有效/无效索引数量
- 清理源文件已移动或删除后的无效索引

### GPU 加速

在 **设置 → 加速** 中检测 DirectML、启用或关闭 GPU。GPU 主要用于图像嵌入和人脸识别；修改后需要重新启动索引任务。探测失败或运行时异常会自动回退 CPU。

### 序列识别

在 **设置 → 序列识别** 中选择严格、平衡、宽松或自定义预设，并调整延时最少帧数、节奏容差和画面距离阈值。数值越严格，越不容易把不连续照片归到同一序列；数值越宽松，召回可能更高但需要更多人工检查。

### 漫游

在 **设置 → 漫游** 中设置闲置等待时间、照片停留时间、自动启用和内容模式。自动漫游只会在应用可见、没有弹窗且没有后台任务时启动。

### 软件更新

在 **设置 → 软件更新** 中可以：

- 开启或关闭自动更新
- 开启或关闭更新提醒
- 手动检查新版本
- 查看下载进度
- 下载完成后重启安装
- 打开 Release 页面手动下载

更新检查和下载会访问更新服务。如果网络无法直接访问 GitHub，可配置 HTTP 代理，例如 `127.0.0.1:7890`，然后点击 **测试** 验证连通性。

### 关于

关于页面显示版本、许可证、作者和开源依赖。第三方模型许可见 [THIRD_PARTY_MODEL_NOTICES.md](THIRD_PARTY_MODEL_NOTICES.md)。

---

## 隐私、网络与诊断

### 本地处理边界

照片扫描、缩略图、EXIF、AI 向量、人脸特征和搜索处理默认在本机完成。应用不会自动上传照片内容。

### 可能发生网络访问的场景

- 启用自动更新或手动检查更新
- 模型缺失/校验失败后执行模型获取
- 用户配置 WebDAV/S3 后主动上传照片或分享页面
- 用户主动打开 GitHub 反馈页面并附加诊断包

### 生成诊断包

出现问题时打开 **设置 → 帮助与诊断**：

1. 描述出错前的最后一步和实际表现。
2. 选择发生频率。
3. 查看将收集的数据说明。
4. 默认只生成脱敏诊断包；如确有需要，再单独选择原生崩溃转储。
5. 导出 ZIP 后检查内容，再决定是否附加到 GitHub Issue。

诊断包可能包含应用版本、Windows/硬件摘要、功能状态和脱敏日志，但不会主动包含照片、数据库、EXIF、人脸/向量数据、搜索词或云端凭据。诊断包只有在用户主动导出或提交时才会离开设备。

---

## 键盘快捷键

按 `?` 可随时打开应用内快捷键帮助。

### 浏览

| 快捷键 | 功能 |
| --- | --- |
| `Space` | 快速预览选中照片 |
| `←` / `→` | 预览或灯箱中切换照片 |
| `↑` / `↓` | 详情面板中切换照片 |
| `Esc` | 关闭预览、灯箱或面板 |
| 双击 | 打开灯箱 |

### 选择与操作

| 快捷键 | 功能 |
| --- | --- |
| 单击 | 选中照片 |
| `Ctrl` + 单击 | 多选或取消选择 |
| `Shift` + 单击 | 范围选择 |
| `Ctrl + A` | 全选 |
| `F` | 收藏/取消收藏 |
| `Delete` | 删除选中照片 |
| `F2` | 批量重命名 |
| `Ctrl + Shift + E` | 导出 |
| `Ctrl + Shift + C` | 格式转换 |

### 界面

| 快捷键 | 功能 |
| --- | --- |
| `I` | 显示/隐藏详情面板 |
| `[` | 折叠/展开侧边栏 |
| `Ctrl + K` | 全局搜索 |
| `?` | 快捷键帮助 |

### 选片

| 快捷键 | PK / Duel | 甄选 / Curate |
| --- | --- | --- |
| `←` | 选左图 | 淘汰 |
| `→` | 选右图 | 保留 |
| `Space` / `↓` | 跳过 | 淘汰 |
| `D` | 平局 | — |
| `S` | — | 跳过相似 |
| `Ctrl + Z` | 撤销 | 撤销 |
| `+` / `-` | 放大/缩小 | 放大/缩小 |
| `0` | 适合屏幕 | 适合屏幕 |

### 灯箱

| 快捷键 | 功能 |
| --- | --- |
| `Space` | 播放/暂停幻灯片 |
| `Esc` | 退出灯箱 |
| `I` | 显示/隐藏详情 |
| `F` | 收藏/取消收藏 |
| `T` | 显示/隐藏缩略图 |
| `R` | 顺时针旋转 90° |
| `Shift + R` | 逆时针旋转 90° |
| `0` / `1` | 适合窗口/实际像素 |

---

## 常见问题

### 搜索为什么没有结果？

先确认搜索模式和筛选条件。语义搜索和以图搜图需要 AI 索引完成；如果侧边栏仍显示处理中，请等待或继续索引。也可以先用文件名、标签、人物和 EXIF 条件验证图库是否已导入。

### AI 模型需要额外下载吗？

正常发布包包含当前版本所需模型。模型缺失、损坏或校验失败时，应用可能执行恢复下载；这时需要网络连接。如果持续失败，请重新安装并检查安全软件拦截。

### 照片被移动后会怎样？

应用不会为任意外部移动建立可靠的路径追踪。移动后的旧记录可能变为无效。在 **设置 → 存储/索引** 中清理无效索引，然后添加新的目标文件夹。

### 数据库和缩略图在哪里？

它们位于 **设置 → 存储/数据目录** 显示的应用数据目录。迁移前请确保目标磁盘有足够空间，迁移期间不要关闭应用。

### 人物分组不准确怎么办？

在人物页面人工命名、合并、隐藏或移除错误照片；必要时使用 **重新聚类**。光线、角度、遮挡、脸部尺寸和照片质量都会影响结果，人物识别不能替代人工确认。

### 人脸识别或 AI 索引很慢怎么办？

确认是否启用了 DirectML，并在设置中重新检测 GPU。GPU 只在支持的硬件和任务上生效，失败时会回退 CPU。大型图库建议使用 SSD 和更多内存，并避免同时运行高负载任务。

### 重复检测没有发现结果？

完全相同的文件可先执行基础扫描；视觉相同和视觉相似检测需要相应 AI/感知哈希处理。检查是否选择了正确的文件夹范围，并等待 AI 索引完成。

### 视觉相似照片可以直接删除吗？

不建议。视觉相似可能只是连拍、裁剪、调色或不同用途的导出版本。请先打开组详情，确认保留者和照片用途，再移入回收站。

### 回收站可以保留多久？

应用回收站默认保留 30 天，之后移动到 Windows 系统回收站。手动清空或提前移入系统回收站后，恢复能力取决于 Windows 回收站和后续操作。

### 为什么应用会联网？

可能原因包括自动更新、手动更新检查、模型恢复下载、用户主动上传到 WebDAV/S3，或用户主动提交诊断包。照片索引和 AI 推理本身默认在本机完成。

### AI Worker 报错或无法处理照片怎么办？

1. 打开 **设置 → 帮助与诊断** 查看并生成脱敏诊断包。
2. 确认 Visual C++ 2015–2022 x64 Redistributable 已安装。
3. 检查杀毒软件是否隔离 `.dll`、`.node` 或 ONNX 相关文件。
4. 重新检测 GPU，必要时关闭 GPU 加速后重试。
5. 使用便携版时，尝试放在不含特殊字符的短路径中运行。
6. 重新安装 v2.0.0，并在提交 Issue 前检查诊断包内容。

### 如何报告问题？

优先在 **设置 → 帮助与诊断** 生成报告，确认脱敏内容后再提交到 [GitHub Issues](https://github.com/Uyoung666/ai-image-manager/issues)。请不要在 Issue、截图或日志中公开云端密码、访问密钥、私密照片或 GPS 信息。

---

## 相关链接

- [项目主页](https://github.com/Uyoung666/ai-image-manager)
- [下载与 Release](https://github.com/Uyoung666/ai-image-manager/releases)
- [English User Guide](GUIDE.en.md)
- [第三方模型声明](THIRD_PARTY_MODEL_NOTICES.md)
- [许可证](LICENSE)
